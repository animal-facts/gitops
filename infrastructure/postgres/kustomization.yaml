apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- secret.yaml
- statefulset.yaml
- service.yaml
- flyway-migrations.yaml

configMapGenerator:
- name: postgres-migrations
  files:
  - migrations/*.sql
